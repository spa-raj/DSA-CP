# Build directories
cmake-build-*/
build/

# CMake generated files
CMakeCache.txt
CMakeFiles/
cmake_install.cmake
Makefile

# IDE / editor
.idea/
.vscode/
.qodo/

# Compiled binaries / objects
*.o
*.obj
*.exe
*.out
*.a
*.so

# Compile your binaries into the build dir (e.g. CLion's cmake-build-*),
# which is already ignored above. If you ever compile manually next to a
# source file, name the output explicitly and add it here.
