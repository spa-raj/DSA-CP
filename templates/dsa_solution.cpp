// Problem : <name>
// Link    : https://leetcode.com/problems/<slug>/
// Summary : <one-line restatement in your own words>
//
// Approach   : <pattern + key idea>
// Complexity : O(?) time, O(?) space
// Date       : YYYY-MM-DD

#include <bits/stdc++.h>
using namespace std;

class Solution {
public:
    // TODO: signature as given by the problem
    int solve() {
        return 0;
    }
};

// Optional local driver — delete or guard out if it clashes with the judge.
int main() {
    Solution s;
    cout << s.solve() << '\n';
    return 0;
}
